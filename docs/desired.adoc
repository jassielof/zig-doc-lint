// The pretty name, and the tagline/subtitle
// Sect anchors and links aren't needed.
= Docent: A Documentation Linter for Zig Projects
Jassiel Ovando <jassielovando@protonmail.com>
v0.1.0, date: main (abc1234)
:toc: auto
:doctype: book


// The abstract could also be the tagline, but it should honor what it is, so it should be a summary of the project or the summary/description of the CLI itself.
[abstract]
--
Docent is a documentation linter for Zig projects.
--

== Usage

[source,sh]
----
docent [OPTIONS] <COMMAND> <paths>...
----

== Arguments
// This argument could be better rendered, for example using ellipses to indicate that it's varidate, or like in HTML forms using an asterisk to indicate that it's required. Figure out what's the best format, and also I don't like that it's using a description list, it'd be better if it's just a section for example
////
=== The argument name
Its description or summary, considering that there might be a short or long one. The summar

==== Examples if there are


////

`<paths>...`::
* Files or directories to lint
* Required: no
* Variadic: yes


== Options
// Same case for global options, not just "Options", as these apply to all commands. Each option should have a better way to indicate if it's required, its help information, etc.

`-r, --rule <rule>=<severity>`::
Override severity: \<name\>=\<allow|warn|deny|forbid\> +
Required: no +
Possible values: allow, warn, deny, forbid +
Scope: local

`--all <VALUE>`::
The level to apply to all rules. +
Required: no +
Possible values: warn, deny +
Scope: local

`-f, --format <VALUE>`::
The output format of the lints. +
Required: no +
Default: `pretty` +
Possible values: pretty, text, minimal, json +
Scope: local

`--include-build-scripts`::
Include build.zig and build/*.zig files in lint targets. +
Required: no +
Default: `false` +
Scope: local

`-h, --help`::
Print help. +
Required: no +
Scope: built-in

`-V, --version`::
Print version. +
Required: no +
Scope: built-in
// Command index is not necessary since AsciiDoc already generates a table of contents, and each command in the command reference has its own summary section.
== Command Index

* xref:cmd-docent[`docent`] - Documentation linter for Zig projects
  ** xref:cmd-docent-docs[`docent docs`] - Generate AsciiDoc documentation for this CLI
  ** xref:cmd-docent-completion[`docent completion`] - Generate shell completion scripts
  ** xref:cmd-docent-help[`docent help`] - Print this message or the help of the given subcommand(s)


== Command Reference
// Seems repetitive to add again the root command usage, considering that there is already a Usage chapter (not section since we are in a book style).
[#cmd-docent]
=== `docent`

Documentation linter for Zig projects

==== Usage

[source,sh]
----
docent [OPTIONS] <COMMAND> <paths>...
----

==== Arguments

`<paths>...`::
Files or directories to lint +
Required: no +
Variadic: yes


==== Options

`-r, --rule <rule>=<severity>`::
Override severity: \<name\>=\<allow|warn|deny|forbid\> +
Required: no +
Possible values: allow, warn, deny, forbid +
Scope: local

`--all <VALUE>`::
The level to apply to all rules. +
Required: no +
Possible values: warn, deny +
Scope: local

`-f, --format <VALUE>`::
The output format of the lints. +
Required: no +
Default: `pretty` +
Possible values: pretty, text, minimal, json +
Scope: local

`--include-build-scripts`::
Include build.zig and build/*.zig files in lint targets. +
Required: no +
Default: `false` +
Scope: local

`-h, --help`::
Print help. +
Required: no +
Scope: built-in

`-V, --version`::
Print version. +
Required: no +
Scope: built-in

==== Subcommands

* xref:cmd-docent-docs[`docent docs`] - Generate AsciiDoc documentation for this CLI
* xref:cmd-docent-completion[`docent completion`] - Generate shell completion scripts
* xref:cmd-docent-help[`docent help`] - Print this message or the help of the given subcommand(s)


[#cmd-docent-docs]
=== `docent docs`

Generate AsciiDoc documentation for this CLI

Parent: xref:cmd-docent[`docent`]

==== Usage

[source,sh]
----
docent docs [OPTIONS]
----

==== Options

`--output-dir <STRING>`::
Directory where AsciiDoc documentation is written. +
Required: no +
Default: `docs` +
Scope: local

`--file <STRING>`::
Output AsciiDoc file name. +
Required: no +
Default: `cli.adoc` +
Scope: local

`--template <STRING>`::
Optional custom Trama template path. +
Required: no +
Scope: local

`-h, --help`::
Print help. +
Required: no +
Scope: built-in


[#cmd-docent-completion]
=== `docent completion`

Generate shell completion scripts

Parent: xref:cmd-docent[`docent`]

==== Usage

[source,sh]
----
docent completion [OPTIONS] <shell>
----

==== Arguments

`<shell>`::
Target shell. +
Required: yes +
Variadic: no
 +
Possible values: bash, zsh, fish, pwsh, nu

==== Options

`-h, --help`::
Print help. +
Required: no +
Scope: built-in


[#cmd-docent-help]
=== `docent help`

Print this message or the help of the given subcommand(s)

Parent: xref:cmd-docent[`docent`]

==== Usage

[source,sh]
----
docent help [COMMAND]...
----


